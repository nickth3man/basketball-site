/**
 * @fileoverview G-League player detail page - placeholder until data is available.
 *
 * @module @/app/gleague/players/[letter]/[id]/page
 */

import { notFound } from 'next/navigation';

/**
 * G-League player detail page. Calls notFound() until G-League player data is available.
 */
export default function GLeaguePlayerPage(): never {
  return notFound();
}
