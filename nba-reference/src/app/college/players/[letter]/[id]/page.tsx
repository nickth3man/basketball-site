/**
 * @fileoverview College basketball player detail page - placeholder until data is available.
 *
 * @module @/app/college/players/[letter]/[id]/page
 */

import { notFound } from 'next/navigation';

/**
 * College basketball player detail page. Calls notFound() until college player data is available.
 */
export default function CollegePlayerPage(): never {
  return notFound();
}
